[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "kpi-anomaly-detector"
version = "0.1.0"
description = "Detects anomalies in FX trade KPI time-series data"
requires-python = ">=3.11"
license = "MIT"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]
