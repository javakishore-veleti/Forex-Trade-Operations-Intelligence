[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "dlq-cluster-analyzer"
version = "0.1.0"
description = "Clusters and categorizes dead-letter-queue errors for FX trade operations"
requires-python = ">=3.11"
license = "MIT"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]
