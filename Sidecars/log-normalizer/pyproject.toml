[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "log-normalizer"
version = "0.1.0"
description = "Normalizes and structures FX trade operation logs for downstream analysis"
requires-python = ">=3.11"
license = "UNLICENSED"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]
