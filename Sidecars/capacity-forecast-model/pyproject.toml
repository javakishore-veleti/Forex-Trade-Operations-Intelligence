[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "capacity-forecast-model"
version = "0.1.0"
description = "Forecasts capacity needs for FX trade infrastructure"
requires-python = ">=3.11"
license = "MIT"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]
