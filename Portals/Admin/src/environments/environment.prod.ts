export const environment = {
  production: true,
  apiBaseUrl: '/api',
  agentWebhookUrl: '/webhook',
  polling: {
    eodDashboardIntervalMs: 30000,
    approvalInboxIntervalMs: 15000,
    exceptionQueueIntervalMs: 30000
  }
};
