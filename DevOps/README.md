# DevOps

Infrastructure-as-code for local and future cloud environments.

- `Local/` — Per-service Docker Compose files, orchestration scripts (`docker-all-up.sh`, `docker-all-down.sh`, `all-status.sh`), and local development stack configuration.
