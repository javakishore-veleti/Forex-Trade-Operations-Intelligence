# .github

GitHub configuration, CODEOWNERS, and CI workflow placeholders.

- `CODEOWNERS` — Maps directory prefixes to responsible teams.
- `workflows/` — CI pipeline definitions (currently placeholder READMEs only; pipelines will be added in a later phase).
