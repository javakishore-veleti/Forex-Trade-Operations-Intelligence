{
  "version": "v1",
  "hooks": [
    {
      "name": "Guard against secrets in implementation files",
      "trigger": "PostFileSave",
      "matcher": "^(Middleware|Agents|Sidecars|DevOps)/.*",
      "action": {
        "type": "command",
        "command": "FILE=\"{{filePath}}\"; SECRETS=$(grep -nEi '(password\\s*=\\s*[^$\\{\"'\''<][^\\n]{3,}|api[_-]?key\\s*=\\s*[^$\\{\"'\''<][^\\n]{3,}|secret\\s*=\\s*[^$\\{\"'\''<][^\\n]{3,}|Bearer [A-Za-z0-9+/]{20,}|jdbc:[a-z]+://[^$\\{][^\\n]*:[^$\\{][^\\n]*@)' \"$FILE\" 2>/dev/null | head -5); if [ -n \"$SECRETS\" ]; then echo \"🚨  POTENTIAL SECRET in $FILE:\"; echo \"$SECRETS\"; echo \"Use environment variable placeholders \\${VAR_NAME} instead of literal values.\"; fi"
      }
    }
  ]
}
