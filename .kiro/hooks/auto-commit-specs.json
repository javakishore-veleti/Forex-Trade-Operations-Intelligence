{
  "version": "v1",
  "hooks": [
    {
      "name": "Auto-commit spec changes",
      "trigger": "PostFileSave",
      "matcher": "\\.kiro/specs/.*\\.md$",
      "action": {
        "type": "command",
        "command": "git add .kiro/specs/ && CHANGED=$(git diff --cached --name-only) && if [ -n \"$CHANGED\" ]; then MSG=$(echo \"$CHANGED\" | head -1 | sed 's|.kiro/specs/||' | sed 's|/requirements.md||' | sed 's|/design.md||' | sed 's|/tasks.md||') && git commit -m \"feat(specs): ${MSG}\"; fi"
      }
    }
  ]
}
